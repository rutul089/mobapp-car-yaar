import React from 'react';
import {View} from 'react-native';
import {Header, SafeAreaWrapper, Text} from '../../../components';

const Edit_Profile_Component = ({params}) => {
  return (
    <SafeAreaWrapper>
      <Header title={'Edit Profile'} />
      <Text>123</Text>
    </SafeAreaWrapper>
  );
};

export default Edit_Profile_Component;
